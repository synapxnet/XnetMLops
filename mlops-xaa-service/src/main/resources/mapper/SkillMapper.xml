<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.synapxnet.mlopsxaaservice.mapper.SkillMapper">

    <resultMap id="SkillResultMap" type="com.synapxnet.mlopsxaaservice.entity.Skill">
        <id property="id" column="id"/>
        <result property="uid" column="uid"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="type" column="type"/>
        <result property="status" column="status"/>
        <result property="category" column="category"/>
        <result property="tags" column="tags"/>
        <result property="contentMd" column="content_md"/>
        <result property="configJson" column="config_json"/>
        <result property="icon" column="icon"/>
        <result property="hasScripts" column="has_scripts"/>
        <result property="hasReferences" column="has_references"/>
        <result property="hasAssets" column="has_assets"/>
        <result property="installCount" column="install_count"/>
        <result property="sourceUrl" column="source_url"/>
        <result property="isOfficial" column="is_official"/>
        <result property="version" column="version"/>
        <result property="creatorId" column="creator_id"/>
        <result property="tenantUid" column="tenant_uid"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.synapxnet.mlopsxaaservice.entity.Skill" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO xnet_mlops_xaa_skill (uid, name, description, type, status, category, tags, content_md,
                               config_json, icon, has_scripts, has_references, has_assets,
                               install_count, source_url, is_official, version, creator_id, tenant_uid, created_at, updated_at)
        VALUES (#{uid}, #{name}, #{description}, #{type}, #{status}, #{category}, #{tags}, #{contentMd},
                #{configJson}, #{icon}, #{hasScripts}, #{hasReferences}, #{hasAssets},
                #{installCount}, #{sourceUrl}, #{isOfficial}, #{version}, #{creatorId}, #{tenantUid}, NOW(), NOW())
    </insert>

    <select id="selectById" resultMap="SkillResultMap">
        SELECT * FROM xnet_mlops_xaa_skill WHERE id = #{id}
    </select>

    <select id="selectByUid" resultMap="SkillResultMap">
        SELECT * FROM xnet_mlops_xaa_skill WHERE uid = #{uid}
    </select>

    <select id="selectAll" resultMap="SkillResultMap">
        SELECT * FROM xnet_mlops_xaa_skill ORDER BY is_official DESC, install_count DESC, created_at DESC
    </select>

    <select id="selectByStatus" resultMap="SkillResultMap">
        SELECT * FROM xnet_mlops_xaa_skill WHERE status = #{status} ORDER BY is_official DESC, install_count DESC, created_at DESC
    </select>

    <select id="selectByType" resultMap="SkillResultMap">
        SELECT * FROM xnet_mlops_xaa_skill WHERE type = #{type} ORDER BY is_official DESC, install_count DESC, created_at DESC
    </select>

    <select id="selectByCategory" resultMap="SkillResultMap">
        SELECT * FROM xnet_mlops_xaa_skill WHERE category = #{category} ORDER BY is_official DESC, install_count DESC, created_at DESC
    </select>

    <select id="selectPublished" resultMap="SkillResultMap">
        SELECT * FROM xnet_mlops_xaa_skill WHERE status = 'published' ORDER BY is_official DESC, install_count DESC, created_at DESC
    </select>

    <select id="selectPublishedByCategory" resultMap="SkillResultMap">
        SELECT * FROM xnet_mlops_xaa_skill WHERE status = 'published' AND category = #{category} ORDER BY is_official DESC, install_count DESC, created_at DESC
    </select>

    <select id="selectOpenXnetCandidatesByCreator" resultMap="SkillResultMap">
        SELECT * FROM xnet_mlops_xaa_skill
        WHERE status = 'draft'
          AND uid LIKE 'OPENXNET-%'
          AND creator_id = #{creatorId}
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        ORDER BY updated_at DESC
    </select>

    <select id="search" resultMap="SkillResultMap">
        SELECT * FROM xnet_mlops_xaa_skill
        WHERE status = 'published'
        <if test="keyword != null and keyword != ''">
            AND (name LIKE CONCAT('%', #{keyword}, '%')
                 OR description LIKE CONCAT('%', #{keyword}, '%')
                 OR tags LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        ORDER BY is_official DESC, install_count DESC, created_at DESC
    </select>

    <select id="selectByIds" resultMap="SkillResultMap">
        SELECT * FROM xnet_mlops_xaa_skill WHERE id IN
        <foreach item="id" collection="ids" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <update id="update" parameterType="com.synapxnet.mlopsxaaservice.entity.Skill">
        UPDATE xnet_mlops_xaa_skill
        SET name = #{name},
            description = #{description},
            type = #{type},
            status = #{status},
            category = #{category},
            tags = #{tags},
            content_md = #{contentMd},
            config_json = #{configJson},
            icon = #{icon},
            has_scripts = #{hasScripts},
            has_references = #{hasReferences},
            has_assets = #{hasAssets},
            source_url = #{sourceUrl},
            is_official = #{isOfficial},
            version = #{version},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="incrementInstallCount">
        UPDATE xnet_mlops_xaa_skill SET install_count = install_count + 1 WHERE id = #{id}
    </update>

    <update id="decrementInstallCount">
        UPDATE xnet_mlops_xaa_skill SET install_count = GREATEST(0, install_count - 1) WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM xnet_mlops_xaa_skill WHERE id = #{id}
    </delete>

</mapper>
