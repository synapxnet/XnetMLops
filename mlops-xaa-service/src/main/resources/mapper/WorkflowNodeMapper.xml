<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.synapxnet.mlopsxaaservice.mapper.WorkflowNodeMapper">

    <resultMap id="WorkflowNodeResultMap" type="com.synapxnet.mlopsxaaservice.entity.WorkflowNode">
        <id property="id" column="id"/>
        <result property="uid" column="uid"/>
        <result property="workflowId" column="workflow_id"/>
        <result property="nodeType" column="node_type"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="configJson" column="config_json"/>
        <result property="positionX" column="position_x"/>
        <result property="positionY" column="position_y"/>
        <result property="width" column="width"/>
        <result property="height" column="height"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.synapxnet.mlopsxaaservice.entity.WorkflowNode" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO xnet_mlops_xaa_workflow_node (uid, workflow_id, node_type, title, description, config_json,
                                       position_x, position_y, width, height, sort_order,
                                       created_at, updated_at)
        VALUES (#{uid}, #{workflowId}, #{nodeType}, #{title}, #{description}, #{configJson},
                #{positionX}, #{positionY}, #{width}, #{height}, #{sortOrder}, NOW(), NOW())
    </insert>

    <insert id="batchInsert" parameterType="list">
        INSERT INTO xnet_mlops_xaa_workflow_node (uid, workflow_id, node_type, title, description, config_json,
                                       position_x, position_y, width, height, sort_order,
                                       created_at, updated_at)
        VALUES
        <foreach collection="nodes" item="node" separator=",">
            (#{node.uid}, #{node.workflowId}, #{node.nodeType}, #{node.title}, #{node.description},
             #{node.configJson}, #{node.positionX}, #{node.positionY}, #{node.width}, #{node.height},
             #{node.sortOrder}, NOW(), NOW())
        </foreach>
    </insert>

    <select id="selectById" resultMap="WorkflowNodeResultMap">
        SELECT * FROM xnet_mlops_xaa_workflow_node WHERE id = #{id}
    </select>

    <select id="selectByUid" resultMap="WorkflowNodeResultMap">
        SELECT * FROM xnet_mlops_xaa_workflow_node WHERE uid = #{uid}
    </select>

    <select id="selectByWorkflowId" resultMap="WorkflowNodeResultMap">
        SELECT * FROM xnet_mlops_xaa_workflow_node WHERE workflow_id = #{workflowId} ORDER BY sort_order ASC
    </select>

    <select id="selectByWorkflowIdAndType" resultMap="WorkflowNodeResultMap">
        SELECT * FROM xnet_mlops_xaa_workflow_node
        WHERE workflow_id = #{workflowId} AND node_type = #{nodeType}
        ORDER BY sort_order ASC
    </select>

    <update id="update" parameterType="com.synapxnet.mlopsxaaservice.entity.WorkflowNode">
        UPDATE xnet_mlops_xaa_workflow_node
        SET node_type = #{nodeType},
            title = #{title},
            description = #{description},
            config_json = #{configJson},
            position_x = #{positionX},
            position_y = #{positionY},
            width = #{width},
            height = #{height},
            sort_order = #{sortOrder},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM xnet_mlops_xaa_workflow_node WHERE id = #{id}
    </delete>

    <delete id="deleteByWorkflowId">
        DELETE FROM xnet_mlops_xaa_workflow_node WHERE workflow_id = #{workflowId}
    </delete>

</mapper>
