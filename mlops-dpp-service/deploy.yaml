---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: xnet-mlops-dpp-service-deployment
  namespace: xnet-mlops
  labels:
    app: xnet-mlops-dpp-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: xnet-mlops-dpp-service
  template:
    metadata:
      labels:
        app: xnet-mlops-dpp-service
    spec:
      imagePullSecrets:
        - name: harbor
      containers:
        - name: xnet-mlops-dpp-service
          image: 192.168.10.132/xnet-mlops/xnet-mlops-dpp-service:${BUILD_NUMBER}
          ports:
            - name: tcp-8182
              containerPort: 8182
              protocol: TCP
          resources:
            limits:
              cpu: "50"
              memory: "16482Mi"
            requests:
              cpu: "500m"
              memory: "512Mi"
          volumeMounts:
            - name: kube-api-access
              mountPath: /var/run/secrets/kubernetes.io/serviceaccount
              readOnly: true
      volumes:
        - name: kube-api-access
          projected:
            sources:
              - serviceAccountToken:
                  expirationSeconds: 3607
                  path: token
              - configMap:
                  name: kube-root-ca.crt
                  items:
                    - key: ca.crt
                      path: ca.crt
              - downwardAPI:
                  items:
                    - path: namespace
                      fieldRef:
                        apiVersion: v1
                        fieldPath: metadata.namespace
            defaultMode: 420
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      serviceAccountName: default
      tolerations:
        - key: node.kubernetes.io/not-ready
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 300
        - key: node.kubernetes.io/unreachable
          operator: Exists
          effect: NoExecute
          tolerationSeconds: 300
---
apiVersion: v1
kind: Service
metadata:
  name: xnet-mlops-dpp-service
  namespace: xnet-mlops
  labels:
    app: xnet-mlops-dpp-service
spec:
  selector:
    app: xnet-mlops-dpp-service
  ports:
    - protocol: TCP
      port: 8182
      targetPort: 8182
      name: tcp-8182
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: xnet-mlops-dpp-service-ingress
  namespace: xnet-mlops
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - http:
        paths:
          - path: /dpp
            pathType: Prefix
            backend:
              service:
                name: xnet-mlops-dpp-service
                port:
                  number: 8182